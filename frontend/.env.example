# Local dev: talk to CarTrack FastAPI directly
VITE_API_URL=http://localhost:8001

# VisionFlow gateway build (npm run build via scripts/build-cartrack-into-visionflow.cmd):
# Same-origin /api + /ws proxied to CARTRACK_BACKEND_URL — leave API URL empty via gateway flag:
# VITE_GATEWAY_MODE=true
