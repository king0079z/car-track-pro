import { CameraCloudWizard } from '../../components/CameraCloudWizard';

/** Settings → Camera cloud — guided wizard (replaces legacy flat form). */
export const DahuaCloudConnect: React.FC = () => <CameraCloudWizard />;
