# Core (plate detection + speed + DB)
opencv-python>=4.8.0
ultralytics>=8.3.0
lap>=0.5.12
numpy>=1.23.0
mysql-connector-python>=8.0.0

# OCR (recommended on Windows; avoids PaddleOCR 3 / OneDNN issues on some PCs)
easyocr>=1.7.0

# Optional — tutorial stack; use only if EasyOCR is removed and your Paddle install works.
# paddlepaddle>=2.6.0
# paddleocr>=2.7.0
