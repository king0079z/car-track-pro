# Copy to .env.deploy.local (git-ignored), fill in values, then:
#   cd scripts
#   .\deploy-everything.ps1 -SkipGitPush
#
# Or export as environment variables before running deploy-everything.ps1

CARTRACK_DUCKDNS_TOKEN=your_duckdns_token_from_top_of_page
CARTRACK_CAMERA_PASSWORD=your_dahua_admin_password_from_dmss

# Optional — Hugging Face upload demo Space
# CARTRACK_HF_TOKEN=hf_xxxxxxxx
# CARTRACK_HF_SPACE=yourname/cartrack

# Defaults (change if needed)
# VPS_IP=46.225.26.2
# DOMAIN=cartrackpro.duckdns.org
